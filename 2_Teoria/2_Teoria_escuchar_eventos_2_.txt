Para gestionar el evento desde la cola de Con eventos.

Lo primero es saber a qué quiero programar el evento.
En este caso es al botón es al que le quiero programar el evento.

Una vez que tengo el evento. Le añado al nodo del evento.
con nodo_boton1.addEventListener("click", poner_rojo);

Cuando le haces la acción. En este caso hacer un click al botón.
Y luego que quiero hacer con el botón: poner en rojo. (poner_rojo.)

y luego declaro la function poner_rojo(){
	let nodos_parrafos = document.getElementsByTagName("p");
	for (const element of nodos:parrafos){

	element.style.color = "red";
	element.style.




}

Cada vez que gestionamos un evento desde la cola de evento, yo quiero saber quien ha generado
el evento. En este caso, se usa el: target.

function = cambia color. 
el e.target sirve para acceder 

Accedo al nodo del botón con nodo_boton1.

e.target = devuelve el nodo de quien genero el evento. Puede ser un botón, 